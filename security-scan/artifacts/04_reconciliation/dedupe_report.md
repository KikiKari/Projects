# Candidate reconciliation

The two raw candidates remain separate. The page-bridge candidate is an unbounded object and storage boundary; the gzip candidate is an independent compressed-message decoder boundary. No raw ids were merged or dropped.
