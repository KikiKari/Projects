#!/usr/bin/env python3
# 1781743218784_260531.js — portiert nach python
# Quelle: javascript, Projects@abstractions:javascript/1781743218784_260531.js
# Erzeugt: 2026-08-23 durch ABSTRACTIONS_MANAGER.py

# 1781743218784_260531.pl — portiert nach javascript
# Quelle: perl5, Projects@abstractions:perl5/1781743218784_260531.pl
# Erzeugt: 2026-08-21 durch ABSTRACTIONS_MANAGER.py

# 1781743218784_260531.py — portiert nach perl5
# Quelle: python, Projects@abstractions:python/1781743218784_260531.py
# Erzeugt: 2026-08-18 durch ABSTRACTIONS_MANAGER.py

import sys
import os

def main():
    args = sys.argv[1:]
    if len(args) != 1:
        print("Usage: python script.py output_file.html")
        sys.exit(1)

    output_file = args[0]

    with open(output_file, 'w', encoding='utf-8') as f:
        # Write DOCTYPE and main script tag
        f.write('<!DOCTYPE html>\n')
        f.write('<script type="application/json" id="cowork-artifact-meta">\n')
        f.write('{\n')
        f.write('  "name": "Secret Vault Public",\n')
        f.write('  "schemaVersion": 1,\n')
        f.write('  "description": "Secret-Vault Public als interaktives Browser-Artefakt: verschlüsselter Secret-Container vollständig client-seitig (WebCrypto, AES-256-GCM + PBKDF2). Öffnen/Anlegen, Anbieter/Felder ergänzen und ersetzen (Rotation), verschlüsseln und als .svpb herunterladen oder Klartext-JSON exportieren. DE/EN nach Browsersprache. Eigenes Format (nicht kompatibel mit dem scrypt-Python-Tool). Keine Secrets eingebettet.",\n')
        f.write('  "mcpTools": [],\n')
        f.write('  "mcpServerNames": []\n')
        f.write('}\n')
        f.write('</script>\n')

        # Write HTML start and head section
        f.write('<html lang="de">\n')
        f.write('<head>\n')
        f.write('<meta charset="utf-8">\n')
        f.write('<meta name="viewport" content="width=device-width, initial-scale=1">\n')
        f.write('<title>Secret-Vault Public</title>\n')
        f.write('<style>\n')
        f.write(':root{ color-scheme:light; --ink:#1b1c1f; --muted:#6c6e75; --faint:#9a9ca3; --card:#fff; --line:#e9eaee; --accent:#5b5bd6; --accent2:#7c5cff; --ok:#22a06b; --err:#e0533d; --radius:16px; --shadow:0 1px 2px rgba(20,20,40,.04),0 6px 20px rgba(20,20,40,.06);}\n')
        f.write('*{box-sizing:border-box;}\n')
        f.write('body{margin:0;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;color:var(--ink);min-height:100vh;background:radial-gradient(1100px 560px at 100% -10%,#e8ecff 0%,rgba(232,236,255,0) 55%),linear-gradient(180deg,#eef1f6,#f7f7f8 42%);background-attachment:fixed;}\n')
        f.write('.wrap{max-width:820px;margin:0 auto;padding:24px 18px 70px;}\n')
        f.write('.brand{display:flex;align-items:center;gap:12px;margin-bottom:4px;}\n')
        f.write('.mark{width:32px;height:32px;border-radius:9px;background:linear-gradient(135deg,var(--accent),var(--accent2));box-shadow:0 4px 12px rgba(91,91,214,.35);position:relative;flex:0 0 auto;}\n')
        f.write('.mark:after{content:"";position:absolute;inset:8px;border-radius:4px;border:2px solid rgba(255,255,255,.92);}\n')
        f.write('h1{font-size:21px;margin:0;font-weight:700;}\n')
        f.write('.sub{color:var(--muted);font-size:13px;margin:2px 0 16px;}\n')
        f.write('.card{background:var(--card);border:1px solid var(--line);border-radius:var(--radius);box-shadow:var(--shadow);padding:16px;margin-bottom:14px;}\n')
        f.write('.card h2{font-size:14px;margin:0 0 10px;}\n')
        f.write('label.lab{display:block;font-size:12px;font-weight:600;color:var(--muted);margin:8px 0 3px;}\n')
        f.write('input,textarea{width:100%;font-size:13px;padding:8px 10px;border:1px solid var(--line);border-radius:9px;font-family:ui-monospace,Menlo,Consolas,monospace;background:#fff;}\n')
        f.write('textarea{min-height:90px;white-space:pre;overflow:auto;}\n')
        f.write('.row{display:flex;gap:8px;flex-wrap:wrap;align-items:center;}\n')
        f.write('.btn{font-size:13px;font-weight:600;padding:8px 14px;border:1px solid var(--line);border-radius:10px;background:#fff;cursor:pointer;box-shadow:var(--shadow);transition:transform .1s;}\n')
        f.write('.btn:hover{transform:translateY(-1px);}\n')
        f.write('.btn.primary{background:linear-gradient(135deg,var(--accent),var(--accent2));color:#fff;border-color:transparent;}\n')
        f.write('.btn.sm{padding:5px 9px;font-size:12px;}\n')
        f.write('.msg{font-size:12px;margin-left:6px;}\n')
        f.write('.msg.ok{color:var(--ok);} .msg.err{color:var(--err);}\n')
        f.write('.prov{border:1px solid var(--line);border-radius:12px;padding:10px 12px;margin-bottom:10px;}\n')
        f.write('.prov h3{margin:0 0 6px;font-size:13.5px;display:flex;align-items:center;gap:8px;}\n')
        f.write('.kv{display:grid;grid-template-columns:180px 1fr auto;gap:6px;margin:4px 0;align-items:center;}\n')
        f.write('.kv input{font-size:12px;padding:5px 7px;}\n')
        f.write('.kv .k{color:var(--muted);font-weight:600;}\n')
        f.write('.muted{color:var(--faint);font-size:13px;}\n')
        f.write('.hide{display:none;}\n')
        f.write('.foot{color:var(--faint);font-size:11.5px;text-align:center;margin-top:18px;line-height:1.5;}\n')
        f.write('a{color:var(--accent);}\n')
        f.write('</style>\n')
        f.write('</head>\n')

        # Write body content
        f.write('<body>\n')
        f.write('<div class="wrap">\n')
        f.write('  <div class="brand"><div class="mark"></div><h1 id="title">Secret-Vault Public</h1></div>\n')
        f.write('  <div class="sub" id="sub">Verschlüsselte Secret-Vault (AES-256-GCM, PBKDF2) — alles im Browser, kein Server.</div>\n')

        # Card: Open or new
        f.write('  <div class="card">\n')
        f.write('    <h2 id="h-open">Öffnen oder neu</h2>\n')
        f.write('    <label class="lab" id="l-pass">Passphrase</label>\n')
        f.write('    <input id="pass" type="password" placeholder="Passphrase…">\n')
        f.write('    <label class="lab" id="l-file">Vault laden (Datei oder Base64 einfügen)</label>\n')
        f.write('    <input id="file" type="file" accept=".svpb,.txt,.vault,.b64">\n')
        f.write('    <textarea id="blob" placeholder="…oder Base64 hier einfügen"></textarea>\n')
        f.write('    <div class="row" style="margin-top:10px">\n')
        f.write('      <button class="btn primary" id="openBtn">Öffnen / Entschlüsseln</button>\n')
        f.write('      <button class="btn" id="newBtn">Neuer leerer Vault</button>\n')
        f.write('      <span class="msg" id="openMsg"></span>\n')
        f.write('    </div>\n')
        f.write('  </div>\n')

        # Card: Editor (hidden by default)
        f.write('  <div class="card hide" id="editor">\n')
        f.write('    <h2 id="h-edit">Inhalt</h2>\n')
        f.write('    <div id="provs"></div>\n')
        f.write('    <div class="row" style="margin-top:8px">\n')
        f.write('      <input id="newProv" placeholder="Neuer Anbieter (Name)" style="max-width:280px">\n')
        f.write('      <button class="btn sm" id="addProvBtn">+ Anbieter</button>\n')
        f.write('    </div>\n')
        f.write('  </div>\n')

        # Card: Save/Export (hidden by default)
        f.write('  <div class="card hide" id="out">\n')
        f.write('    <h2 id="h-save">Speichern / Export</h2>\n')
        f.write('    <div class="row">\n')
        f.write('      <button class="btn primary" id="encBtn">Verschlüsseln</button>\n')
        f.write('      <button class="btn" id="dlBtn">Als .svpb herunterladen</button>\n')
        f.write('      <button class="btn" id="expBtn">Klartext-JSON exportieren</button>\n')
        f.write('      <span class="msg" id="saveMsg"></span>\n')
        f.write('    </div>\n')
        f.write('    <label class="lab" id="l-result">Ergebnis (zum Kopieren/Speichern)</label>\n')
        f.write('    <textarea id="result" readonly></textarea>\n')
        f.write('  </div>\n')

        # Footer
        f.write('  <div class="foot" id="foot"></div>\n')
        f.write('</div>\n')

        # JavaScript section
        f.write('<script>\n')
        f.write('const L = ((navigator.language||"en").toLowerCase().startsWith("de"))?"de":"en";\n')
        f.write('const T = {\n')
        f.write(' title:{de:"Secret-Vault Public",en:"Secret-Vault Public"},\n')
        f.write(' sub:{de:"Verschlüsselte Secret-Vault (AES-256-GCM, PBKDF2) — alles im Browser, kein Server.",en:"Encrypted secret vault (AES-256-GCM, PBKDF2) — fully in the browser, no server."},\n')
        f.write(' hOpen:{de:"Öffnen oder neu",en:"Open or new"},\n')
        f.write(' pass:{de:"Passphrase",en:"Passphrase"},\n')
        f.write(' file:{de:"Vault laden (Datei oder Base64 einfügen)",en:"Load vault (file or paste Base64)"},\n')
        f.write(' blob:{de:"…oder Base64 hier einfügen",en:"…or paste Base64 here"},\n')
        f.write(' open:{de:"Öffnen / Entschlüsseln",en:"Open / Decrypt"},\n')
        f.write(' neu:{de:"Neuer leerer Vault",en:"New empty vault"},\n')
        f.write(' hEdit:{de:"Inhalt",en:"Content"},\n')
        f.write(' newProv:{de:"Neuer Anbieter (Name)",en:"New provider (name)"},\n')
        f.write(' addProv:{de:"+ Anbieter",en:"+ Provider"},\n')
        f.write(' hSave:{de:"Speichern / Export",en:"Save / Export"},\n')
        f.write(' enc:{de:"Verschlüsseln",en:"Encrypt"},\n')
        f.write(' dl:{de:"Als .svpb herunterladen",en:"Download as .svpb"},\n')
        f.write(' exp:{de:"Klartext-JSON exportieren",en:"Export plaintext JSON"},\n')
        f.write(' result:{de:"Ergebnis (zum Kopieren/Speichern)",en:"Result (to copy/save)"},\n')
        f.write(' foot:{de:"Eigenes Format (PBKDF2). Nicht kompatibel mit dem scrypt-Python-Tool. Sicherheit liegt in der Passphrase; Inhalt ohne sie nicht wiederherstellbar.",en:"Own format (PBKDF2). Not compatible with the scrypt Python tool. Security rests on the passphrase; content is unrecoverable without it."},\n')
        f.write(' needPass:{de:"Passphrase eingeben.",en:"Enter a passphrase."},\n')
        f.write(' noInput:{de:"Datei laden oder Base64 einfügen.",en:"Load a file or paste Base64."},\n')
        f.write(' bad:{de:"Falsche Passphrase oder ungültiger Vault.",en:"Wrong passphrase or invalid vault."},\n')
        f.write(' opened:{de:"Geöffnet.",en:"Opened."},\n')
        f.write(' created:{de:"Neuer Vault angelegt.",en:"New vault created."},\n')
        f.write(' encrypted:{de:"Verschlüsselt — unten kopieren oder herunterladen.",en:"Encrypted — copy below or download."},\n')
        f.write(' needOpen:{de:"Erst öffnen/anlegen.",en:"Open/create first."},\n')
        f.write(' field:{de:"Feld",en:"field"}, value:{de:"Wert",en:"value"},\n')
        f.write(' addField:{de:"+ Feld",en:"+ field"}, del:{de:"✕",en:"✕"},\n')
        f.write(' newField:{de:"neues Feld",en:"new field"}, newValue:{de:"Wert",en:"value"}\n')
        f.write('};\n')
        f.write('const tr=k=>T[k][L];\n')
        f.write('// apply static i18n\n')
        f.write('title.textContent=tr("title"); sub.textContent=tr("sub"); document.title=tr("title");\n')
        f.write('document.getElementById("h-open").textContent=tr("hOpen");\n')
        f.write('document.getElementById("l-pass").textContent=tr("pass");\n')
        f.write('document.getElementById("l-file").textContent=tr("file");\n')
        f.write('blob.placeholder=tr("blob");\n')
        f.write('openBtn.textContent=tr("open"); newBtn.textContent=tr("neu");\n')
        f.write('document.getElementById("h-edit").textContent=tr("hEdit");\n')
        f.write('newProv.placeholder=tr("newProv"); addProvBtn.textContent=tr("addProv");\n')
        f.write('document.getElementById("h-save").textContent=tr("hSave");\n')
        f.write('encBtn.textContent=tr("enc"); dlBtn.textContent=tr("dl"); expBtn.textContent=tr("exp");\n')
        f.write('document.getElementById("l-result").textContent=tr("result");\n')
        f.write('foot.textContent=tr("foot");\n')
        f.write('\n')
        f.write('let VAULT=null; // {meta, providers:{}}\n')
        f.write('\n')
        f.write('const enc=new TextEncoder(), dec=new TextDecoder();\n')
        f.write('function u8b64(u8){ let s=""; for(let i=0;i<u8.length;i+=0x8000) s+=String.fromCharCode.apply(null,u8.subarray(i,i+0x8000)); return btoa(s); }\n')
        f.write('function b64u8(b64){ const s=atob(b64.trim()); const u=new Uint8Array(s.length); for(let i=0;i<s.length;i++) u[i]=s.charCodeAt(i); return u; }\n')
        f.write('async function deriveKey(pw,salt){\n')
        f.write('  const km=await crypto.subtle.importKey("raw",enc.encode(pw),"PBKDF2",false,["deriveKey"]);\n')
        f.write('  return crypto.subtle.deriveKey({name:"PBKDF2",salt,iterations:210000,hash:"SHA-256"},km,{name:"AES-GCM",length:256},false,["encrypt","decrypt"]);\n')
        f.write('}\n')
        f.write('async function encryptObj(obj,pw){\n')
        f.write('  const salt=crypto.getRandomValues(new Uint8Array(16)), iv=crypto.getRandomValues(new Uint8Array(12));\n')
        f.write('  const key=await deriveKey(pw,salt);\n')
        f.write('  const ct=new Uint8Array(await crypto.subtle.encrypt({name:"AES-GCM",iv},key,enc.encode(JSON.stringify(obj,null,2))));\n')
        f.write('  const magic=enc.encode("SVPB1"); const out=new Uint8Array(5+16+12+ct.length);\n')
        f.write('  out.set(magic,0); out.set(salt,5); out.set(iv,21); out.set(ct,33); return u8b64(out);\n')
        f.write('}\n')
        f.write('async function decryptB64(b64,pw){\n')
        f.write('  const raw=b64u8(b64); if(dec.decode(raw.slice(0,5))!=="SVPB1") throw new Error("magic");\n')
        f.write('  const key=await deriveKey(pw,raw.slice(5,21));\n')
        f.write('  const pt=await crypto.subtle.decrypt({name:"AES-GCM",iv:raw.slice(21,33)},key,raw.slice(33));\n')
        f.write('  return JSON.parse(dec.decode(pt));\n')
        f.write('}\n')
        f.write('function esc(s){return (s==null?"":String(s)).replace(/[&<>"]/g,c=>({\'&\':\'&amp;\',\'<\':\'&lt;\',\'>\':\'&gt;\',\'"\':\'&quot;\'}[c]));}\n')
        f.write('\n')
        f.write('function renderEditor(){\n')
        f.write('  document.getElementById("editor").classList.remove("hide");\n')
        f.write('  document.getElementById("out").classList.remove("hide");\n')
        f.write('  const P=VAULT.providers||{}; const root=document.getElementById("provs"); root.innerHTML="";\n')
        f.write('  Object.keys(P).forEach(name=>{\n')
        f.write('    const d=document.createElement("div"); d.className="prov";\n')
        f.write('    let rows="";\n')
        f.write('    Object.keys(P[name]).forEach(k=>{ rows+=`<div class="kv"><span class="k">${esc(k)}</span><input data-p="${esc(name)}" data-k="${esc(k)}" value="${esc(P[name][k])}"><button class="btn sm" data-del="${esc(name)}|${esc(k)}">${tr("del")}</button></div>`; });\n')
        f.write('    d.innerHTML=`<h3>${esc(name)} <button class="btn sm" data-delp="${esc(name)}">${tr("del")}</button></h3>${rows}\n')
        f.write('      <div class="row" style="margin-top:6px"><input class="nf" data-np="${esc(name)}" placeholder="${tr("newField")}" style="max-width:180px"><input class="nv" data-np="${esc(name)}" placeholder="${tr("newValue")}" style="max-width:260px"><button class="btn sm" data-addf="${esc(name)}">${tr("addField")}</button></div>`;\n')
        f.write('    root.appendChild(d);\n')
        f.write('  });\n')
        f.write('  root.querySelectorAll("input[data-k]").forEach(i=>i.onchange=()=>{ VAULT.providers[i.dataset.p][i.dataset.k]=i.value; });\n')
        f.write('  root.querySelectorAll("button[data-del]").forEach(b=>b.onclick=()=>{ const [p,k]=b.dataset.del.split("|"); delete VAULT.providers[p][k]; renderEditor(); });\n')
        f.write('  root.querySelectorAll("button[data-delp]").forEach(b=>b.onclick=()=>{ delete VAULT.providers[b.dataset.delp]; renderEditor(); });\n')
        f.write('  root.querySelectorAll("button[data-addf]").forEach(b=>b.onclick=()=>{ const p=b.dataset.addf; const nf=root.querySelector(`.nf[data-np="${CSS.escape(p)}"]`).value.trim(); const nv=root.querySelector(`.nv[data-np="${CSS.escape(p)}"]`).value; if(nf){ VAULT.providers[p][nf]=nv; renderEditor(); } });\n')
        f.write('}\n')
        f.write('\n')
        f.write('document.getElementById("file").onchange=e=>{ const f=e.target.files[0]; if(!f)return; const r=new FileReader(); r.onload=()=>{ blob.value=r.result.trim(); }; r.readAsText(f); };\n')
        f.write('openBtn.onclick=async()=>{\n')
        f.write('  const m=document.getElementById("openMsg"); m.className="msg"; m.textContent="";\n')
        f.write('  if(!pass.value){ m.className="msg err"; m.textContent=tr("needPass"); return; }\n')
        f.write('  if(!blob.value.trim()){ m.className="msg err"; m.textContent=tr("noInput"); return; }\n')
        f.write('  try{ VAULT=await decryptB64(blob.value,pass.value); if(!VAULT.providers)VAULT.providers={}; renderEditor(); m.className="msg ok"; m.textContent=tr("opened"); }\n')
        f.write('  catch(err){ m.className="msg err"; m.textContent=tr("bad"); }\n')
        f.write('};\n')
        f.write('newBtn.onclick=()=>{\n')
        f.write('  const m=document.getElementById("openMsg");\n')
        f.write('  if(!pass.value){ m.className="msg err"; m.textContent=tr("needPass"); return; }\n')
        f.write('  VAULT={meta:{created:new Date().toISOString().slice(0,10),format:"SVPB1"},providers:{}}; renderEditor();\n')
        f.write('  m.className="msg ok"; m.textContent=tr("created");\n')
        f.write('};\n')
        f.write('addProvBtn.onclick=()=>{ if(!VAULT){ return; } const n=newProv.value.trim(); if(n){ VAULT.providers[n]=VAULT.providers[n]||{}; newProv.value=""; renderEditor(); } };\n')
        f.write('encBtn.onclick=async()=>{\n')
        f.write('  const m=document.getElementById("saveMsg"); m.className="msg";\n')
        f.write('  if(!VAULT){ m.className="msg err"; m.textContent=tr("needOpen"); return; }\n')
        f.write('  if(!pass.value){ m.className="msg err"; m.textContent=tr("needPass"); return; }\n')
        f.write('  result.value=await encryptObj(VAULT,pass.value); m.className="msg ok"; m.textContent=tr("encrypted");\n')
        f.write('};\n')
        f.write('dlBtn.onclick=()=>{ if(!result.value)return; try{ const b=new Blob([result.value],{type:"text/plain"}); const u=URL.createObjectURL(b); const a=document.createElement("a"); a.href=u; a.download="vault.svpb"; document.body.appendChild(a); a.click(); a.remove(); setTimeout(()=>URL.revokeObjectURL(u),1500);}catch(e){} };\n')
        f.write('expBtn.onclick=()=>{ if(!VAULT)return; result.value=JSON.stringify(VAULT,null,2); };\n')
        f.write('</script>\n')
        f.write('</body>\n')
        f.write('</html>\n')

    print(f'HTML file generated: {output_file}')

if __name__ == '__main__':
    main()
